<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huangsf.ums.mapper.sys.MenuMapper">


    <select id="getVisiableMenu"
            resultMap="BaseResultMap">
        SELECT
        <include refid="V_Column_List"/>
        from (
        SELECT
        <include refid="V_Column_List"/>
        from menu where is_enable = true and is_public = true
        UNION ALL
        SELECT
        <include refid="V_Column_List"/>
        from menu where is_enable = true and is_public = false
        and id in (
        SELECT distinct authority_id FROM role_authority ra
        INNER JOIN user_role ur on ra.role_id = ur.role_id
        INNER JOIN role r on r.id = ra.role_id
        where ur.user_id = #{userId, jdbcType=BIGINT} and r.status = true
        and ra.authority_type = 'MENU'
        )
        ) tmp ORDER BY sort_value asc
    </select>

    <!--    <select id="findMenuIdByResourceId" parameterType="map" resultType="java.lang.Long">-->
<!--        SELECT DISTINCT menu_id from pd_auth_resource where id in-->
<!--        <foreach close=")" collection="resourceIdList" item="id" open="(" separator=",">-->
<!--            #{id}-->
<!--        </foreach>-->
<!--    </select>-->

</mapper>
